/*
	Given a string. Return a Array of all possible combinations.

	Ex:
		INPUT: string => "abc"

		OUTPUT: Array of strings 
			{ "a", "b", "c", "ab", "ac", "bc", "abc" }

		NOTE: 
			(1) Dont Worry about order of the strings in the array.
			(2) Convert the given string to lowercase and continue
			(3) Dont use any global declarations
			(4) Dont use string.h functions
			(5) Assume there are no duplicates
*/

#include <stdio.h>
#include <stdlib.h>

char **combinationsof(char *str)
{
	return NULL;
}

/*

	As there are recursive and non-recursive solutions, Write the second solution here.



*/
