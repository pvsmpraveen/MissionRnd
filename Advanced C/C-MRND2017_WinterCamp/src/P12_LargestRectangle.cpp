#include<stdio.h>

/*

	Given a 2D map with 0s and 1s.
	Find the first largest rectangle filled with 1s.
	RETURN 1 if there is a rectangle, 0 otherwise.
	Place the row, column of the top-left 1 in 'row' and 'col'.
	Place the height and width of the rectangle in 'height' and 'width'.
	If 'row' or 'col' is NULL, then assume that the caller decided that
	he/she doesn't need that value.

*/


int largestRectangle(int *map, int nRows, int nCols, int *row, int *col, int *height, int *width) {
	return 0;
}