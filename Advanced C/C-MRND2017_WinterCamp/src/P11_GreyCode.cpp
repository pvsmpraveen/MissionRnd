#include<stdio.h>


/*

	Given a natural number. 
	Generate Gray Codes with 'nBits' with any two adjacent codes
	differing by exactly one bit.
	Store the codes in 'codes'
	Return 1 on success, 0 on failure.
	codes' will already have memory for 2^nBits codes.

*/

int makeGrayCodesSeq(int *codes, int nBits) {
	return 0;
}